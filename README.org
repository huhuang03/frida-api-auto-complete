This is for auto complete for frida script

* How to use
add some thing like this in your =package.json= and then run =npm install= to install
#+BEGIN_SRC json
{
  "devDependencies": {
    "frida-api-auto-complte": "../../frida-api-auto-complete"
  }
}
#+END_SRC
